{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Training",
            "type": "debugpy",
            "request": "launch",
            "module": "scripts.run_training",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
